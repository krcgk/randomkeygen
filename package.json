{
  "name": "randomkeygen",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/randomkeygen.js",
  "bin": {
    "randomkeygen": "./dist/randomkeygen.js"
  },
  "scripts": {
    "create": "npm run build && npm run test",
    "build": "tsc -p .",
    "test": "npm i -g && randomkeygen",
    "refresh": "rm -rf ./node_modules ./package-lock.json && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^2.20.0"
  },
  "devDependencies": {
    "@types/node": "^11.13.2",
    "ts-node": "^8.0.3",
    "typescript": "^3.4.2"
  }
}
